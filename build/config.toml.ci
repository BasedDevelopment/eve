# Move this file to /etc/eve/config.toml

hostname = "eveCI"
tls_path = "/etc/eve/tls"

[api]
host = "0.0.0.0"
port = 3000
behind_proxy = false

[database]
url = "postgres://postgres:password@localhost:5432/postgres"
